// want to map unique categories (somewhat like uniqueDays) from API
// if indexOf() returns false, push to options
// keep track in App.js this.state
export const options = [
  // options
  (teamsOptions: []),
  (locationsOptions: []),
  (itemTypeOptions: []),
  (gameTypeOptions: []),
  (gameStateOptions: [])
];

// filter function that evaluates e.target.value of <select> button on handleChange(e) {} callback
